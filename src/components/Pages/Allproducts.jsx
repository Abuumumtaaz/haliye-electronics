import React from 'react'

const Allproducts = (prop) => {
    const {id, name, price, image, slug} = data;
  return (
    <div>
        <div className="itemy">
            <img src={image} alt="scss" />
        </div>
    </div>
  )
}

export default Allproducts